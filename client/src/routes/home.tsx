import Logo from '../components/ui/icons';
import AppLayout from '../layouts/app-layout';

const Home = () => {
  return (
    <AppLayout>
      Hello home <Logo className='size-7' />
    </AppLayout>
  );
};

export default Home;
